% 系统架构图 - RL+飞行控制整体框架
\begin{tikzpicture}[
    node distance=1.5cm,
    box/.style={rectangle, rounded corners, draw=black!60, fill=blue!5, 
                minimum width=2.5cm, minimum height=1cm, text centered, font=\small},
    arrow/.style={-{Stealth[length=2mm]}, thick, black!60},
    env/.style={rectangle, rounded corners, draw=green!60!black, fill=green!5,
                minimum width=3cm, minimum height=1.2cm, text centered, font=\small},
    agent/.style={rectangle, rounded corners, draw=red!60!black, fill=red!5,
                  minimum width=3cm, minimum height=1.2cm, text centered, font=\small}
]

% Agent
\node[agent] (agent) at (0,0) {\textbf{RL Agent}\\Policy Network};

% Environment
\node[env] (env) at (6,0) {\textbf{UAV Environment}\\Dynamics + Sensors};

% State
\node[box] (state) at (3,2) {State $s_t$\\Position, Velocity, Attitude};

% Action
\node[box] (action) at (3,-2) {Action $a_t$\\Motor Commands};

% Reward
\node[box] (reward) at (0,-3.5) {Reward $r_t$\\Tracking Error + Penalties};

% Arrows
\draw[arrow] (state) -- (agent);
\draw[arrow] (agent) -- (action);
\draw[arrow] (action) -- (env);
\draw[arrow] (env) -- (state);
\draw[arrow, dashed] (env) -- (reward);
\draw[arrow, dashed] (reward) -| (agent);

% Labels
\node[font=\footnotesize] at (1.5,1.5) {Observation};
\node[font=\footnotesize] at (4.5,-1.5) {Control Input};

\end{tikzpicture}
